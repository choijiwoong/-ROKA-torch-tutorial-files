#Height, Weight, Channel
#Convolution operation: Step, feature map, stride
#padding
#convolution layer
#bias
#how to calculate the size of feature map(input size, kernal size, sitride)
#channel size of input data == channel size of kernal
#Pooling(max pooling, average pooling)
